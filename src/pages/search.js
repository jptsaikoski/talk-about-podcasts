import './discover.css';

export default function SearchPage(props) {
    return (
        <div style={{
            backgroundColor: "blue",
            width: "100%",
            height: "100%"
        }}>Search</div>
    );
}